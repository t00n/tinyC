
int tiny() {
    int * p;
    int * q;
    int a[5];
    int b[5];
    int x;
    int y;
    // p = &p;
    // p = &a;
    // p = x;
    // p = a[1];
    // p = q[2];
    // *p = a;
    // *p = q;
    // a = *p;
    // a = a[5];
    // *a = p;
    // *a = b;
    // *x = y;
    // *x = *y;
    // x = p;
    // x = a;
}